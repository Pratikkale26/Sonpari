import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sonpari - Social Gold Savings App for Families",
  description: "Save small amounts of gold daily with family and friends. Build saving streaks together. Gift accumulated gold on birthdays, festivals, and special occasions.",
  keywords: ["gold savings", "digital gold", "family savings", "gold gifting", "micro savings", "Indian families"],
  openGraph: {
    title: "Sonpari - Social Gold Savings App",
    description: "Turn everyday savings into meaningful gold gifts",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
